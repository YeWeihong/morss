# Morss 环境变量配置示例
# Morss Environment Variables Example Configuration
#
# 使用方法 / Usage:
#   cp .env.example .env
#   编辑 .env 文件设置你需要的值 / Edit .env to set your values
#   docker-compose up -d

# ============================================
# 端口配置 / Port Configuration
# ============================================

# 主机端口（外部访问端口）/ Host port (external access port)
# 默认: 8000 / Default: 8000
# 示例: 修改为 18000 / Example: Change to 18000
HOST_PORT=8000

# ============================================
# 调试模式 / Debug Mode
# ============================================

# 启用调试模式（生产环境建议设为 0）
# Enable debug mode (recommended to set to 0 in production)
# 0 = 关闭 / Off, 1 = 开启 / On
DEBUG=0

# ============================================
# 缓存配置 / Cache Configuration
# ============================================

# 缓存类型 / Cache type
# 选项 / Options: diskcache, redis, 或留空使用内存缓存 / or leave empty for in-memory cache
CACHE=diskcache

# 磁盘缓存目录 / Disk cache directory
# 仅当 CACHE=diskcache 时有效 / Only valid when CACHE=diskcache
DISKCACHE_DIR=/cache

# 缓存大小（字节）/ Cache size (bytes)
# 1073741824 = 1GB
CACHE_SIZE=1073741824

# Redis 配置（仅当 CACHE=redis 时需要）
# Redis configuration (only needed when CACHE=redis)
# REDIS_HOST=redis
# REDIS_PORT=6379
# REDIS_DB=0

# ============================================
# 性能配置 / Performance Configuration
# ============================================

# 每个 Feed 最大文章数 / Maximum items per feed
MAX_ITEM=100

# 最大处理时间（秒）/ Maximum processing time (seconds)
MAX_TIME=60

# HTTP 请求超时（秒）/ HTTP request timeout (seconds)
TIMEOUT=15

# ============================================
# Gunicorn 配置 / Gunicorn Configuration
# ============================================

# Gunicorn 命令行参数 / Gunicorn command line arguments
# --workers: Worker 进程数 / Number of worker processes
# --timeout: 请求超时时间 / Request timeout
GUNICORN_CMD_ARGS=--workers=4 --timeout=120
